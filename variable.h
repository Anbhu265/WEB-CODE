/*#define Reset 1<<16
unsigned char RX_Buffer[100];
unsigned char c;
//unsigned char LEDON='@', LEDOFF='%';
unsigned int Buffer_Size=100,RX_INDEX=0;
unsigned char COMMAND[1];
char ip_address[16];  // Buffer to hold the extracted IP address
int retries=3;      // Number of retries for critical commands
volatile int reset_flag = 0;
*/
